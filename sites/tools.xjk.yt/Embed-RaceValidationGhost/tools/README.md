# Required Binaries

Place these binaries in this directory on the server:

- `EmbedRaceValidationGhost.exe`
- `ReplayDataExtractor.exe`

Optional:

- `gbxlzo.exe` (if not using the copy from `Strip-RaceValidationGhost/tools`)

The backend defaults now check this folder first.

