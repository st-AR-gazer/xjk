# Replay-Verification

Launch/discovery entry under `tools.xjk.yt` for the canonical Validifier product.

## What it does

- Hosts the lightweight launch page at `https://tools.xjk.yt/Replay-Verification/`
- Redirects users into `https://validifier.xjk.yt/` with record/map query parameters
- Does not define the canonical public API boundary

## Canonical product

- Website: `https://validifier.xjk.yt/`
- Public API: `https://validifier.xjk.yt/api/v1/...`
