# Legacy Folder

The active tracker backend now lives at:

- `services/tracker`

`backend.legacy/` is kept only as a temporary backup from the migration and is not used by local or server startup scripts.
