# plugins.xjk.yt Workspace

This directory contains everything for the `plugins.xjk.yt` subdomain.

Current app:

- `Plugins-Hub` -> served at `https://plugins.xjk.yt/`

For plugin catalog updates, edit:

- `Plugins-Hub/data/plugins.json`
